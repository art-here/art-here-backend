---
spring:
  config:
    activate:
      on-profile: set1
server:
  port: 8081

---
spring:
  config:
    activate:
      on-profile: set2
server:
  port: 8082

---
spring:
  config:
    activate:
      on-profile: real-db

  jpa:
    show-sql: true
  datasource:
        driver-class-name: org.h2.Driver # 시작할때 실행시킬 script

jasypt:
  encryptor:
    password: art_here_jasypt_key

management:
  endpoints:
    web:
      base-path: /actuator
      exposure:
        include: "*"
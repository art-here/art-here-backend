spring:
  jpa:
    show-sql: true

jasypt:
  encryptor:
    password: art_here_jasypt_key

management:
  endpoints:
    web:
      path-mapping:
        health: health_check
      exposure:
        include: "*"

---
spring:
  profiles:
    active: local

---
spring:
  profiles:
    group:
      set1: real-db
      set2: real-db
